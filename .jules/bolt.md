## 2026-05-30 - Dependency overrides in Monorepo
**Learning:** Monorepo package dependencies mismatch causes npm install and Expo export failures (like `EOVERRIDE`).
**Action:** Always verify `react-native-reanimated`, `react-native-safe-area-context`, and `react-native-screens` match their overrides.
