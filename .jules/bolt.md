## 2026-06-21 - [Dependency Conflicts in Monorepo]
**Learning:** React native components conflict when using `^` in version specifications in overrides.
**Action:** Always follow the memory instructions and remove `^` for specific native Expo modules.
