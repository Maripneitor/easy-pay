/**
 * @deprecated
 * Este archivo ha sido eliminado para forzar el uso de la Arquitectura Hexagonal.
 * 
 * Por favor use:
 * - `useDependencies()` del `DependenciesContext` para acceder a los Casos de Uso.
 * - Los paquetes de `@easy-pay/domain` para modelos y lógica de negocio.
 * 
 * Si necesita realizar una petición directa (no recomendado), use `infrastructure/api/http-client`.
 */
export {};
